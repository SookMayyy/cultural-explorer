# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/settings.json
*.swp
*.swo

# Build output
dist/
build/

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log

# Claude Code project instructions (local only)
CLAUDE.md
.claude/

# seed-content.mjs temp ESM twins
scripts/.gen/

# Voice-over audition/pitch previews (scratch — not part of the app)
pitch-previews/
# Natural-pitch master clips used by `npm run repitch` (regenerated on demand)
src/assets/audio/vo_master/

# Retired files kept locally for reference only — they are already in git
# history (see archive/README.md), so the repo doesn't need a second copy.
archive/
